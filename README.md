# ClinicalFlow

**FHIR-based EHR pipeline and cardiovascular cohort analysis.**

ClinicalFlow ingests synthetic electronic health records in HL7 FHIR R4 format,
builds a reproducible data-processing and quality-assurance pipeline over them,
and analyzes a cardiovascular cohort for population-health insights — surfaced
through a Streamlit dashboard.

> ⚠️ **Synthetic data only.** All patient data is generated by
> [Synthea](https://github.com/synthetichealth/synthea) and contains **no real
> patients and no PHI**. Nothing here is derived from real clinical records.

---

## Status

Project is under active construction, built phase by phase:

- [x] **Phase 0** — Project setup (scaffold, deps, Makefile, logging)
- [ ] **Phase 1** — Data acquisition (Synthea generator, seed 1234, ~3000 patients)
- [ ] **Phase 2** — Ingestion (FHIR R4 → DuckDB)
- [ ] **Phase 3** — Data governance & QA
- [ ] **Phase 4** — Cardiovascular cohort & analysis
- [ ] **Phase 5** — Streamlit dashboard
- [ ] **Phase 6** — Tests, README, polish

---

## Prerequisites

- **Python 3.10+** (developed on 3.10.11).
- **Java 11+** (Java 17+ recommended; required only to run the Synthea generator).

## Setup

```bash
python -m venv .venv
# Windows:   .\.venv\Scripts\Activate.ps1
# Unix/Mac:  source .venv/bin/activate
pip install -r requirements.txt
pip install -e .
```

## Running

This repo ships a `Makefile` (Unix/WSL/git-bash) and an equivalent
`make.ps1` for Windows PowerShell. Pick whichever matches your shell:

| Task | Make (Unix) | PowerShell (Windows) |
|---|---|---|
| Install deps | `make setup` | `.\make.ps1 setup` |
| Get data | `make data` | `.\make.ps1 data` |
| Ingest | `make ingest` | `.\make.ps1 ingest` |
| QA | `make qa` | `.\make.ps1 qa` |
| Cohort | `make cohort` | `.\make.ps1 cohort` |
| Full pipeline | `make all` | `.\make.ps1 all` |
| Dashboard | `make dashboard` | `.\make.ps1 dashboard` |
| Tests | `make test` | `.\make.ps1 test` |

## Data reproducibility

Generated with Synthea using a fixed seed for reproducibility:

- Seed: `1234`
- Population: `3000`
- State: `Massachusetts`

Details and the exact command are documented in Phase 1 (see Status above).

## Architecture

```
raw FHIR bundles  ->  ingest  ->  DuckDB  ->  QA checks  ->  cohort analysis  ->  dashboard
                                     |            |               |
                                     |       qa_results      cv_cohort
                                     |     data_quality_     cohort_summary.md
                                     |       report.md
```

## License / data disclaimer

Synthetic data only — generated by Synthea, contains no PHI. See the disclaimer
at the top of this file.
